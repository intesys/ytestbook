import { createStyles } from "@mantine/core";

export const useTableStyles = createStyles((theme) => ({
  tbody: {
    backgroundColor: "#ffffff"
  }
}));

export const useAddUseCaseStyles = createStyles((theme) => ({
  textInput: {
    input: {
      border: "none"
    }
  }
}))