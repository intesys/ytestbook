import { createStyles } from "@mantine/core";

const useStyles = createStyles((theme) => ({
  root: {
    position: "relative",
    padding: "10px 14px",
    height: "auto",
  },
  leftIcon: {
    svg: {
      width: "18px",
    },
  },
  rightIcon: {
    svg: {
      width: "18px",
    },
  },
}));

export default useStyles;
